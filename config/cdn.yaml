kind: "CDN"
version: "1"
data:
  originSelectors:
    rules:
      - name: route-to-asset-search
        when: { reqProperty: path, equals: "/api/asset-search" }
        action:
          type: selectAemOrigin
          originName: edgefunction-asset-search
